import styled from 'styled-components';

export const Footer = styled.footer`
  display: flex;
  justify-content: center;
  align-items: center;
  background-color: #ffffff;
  color: #47465f;
  padding: 10px;
  font-size: 14px;
  font-weight: bold;
  box-shadow: 0px -2px 6px rgba(0, 0, 0, 0.1);
  width: 100%;
  position: fixed;
  bottom: 0;
  left: 0;
`;

export const ParrotLogoFooter = styled.img`
  height: 20px;
`;

export const PoweredByText = styled.span`
  margin-right: 8px;
  color: #47465f;
`;
